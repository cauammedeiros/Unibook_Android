<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/erro_fundo" /> <!-- Fundo rosa claro -->
    <stroke android:width="1dp" android:color="@color/erro_borda" /> <!-- Borda vermelha -->
    <corners android:radius="8dp" />
</shape>